datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:11190826ISGIS@localhost:1234/test
")
}

generator client {
  provider = "prisma-client-js"
}


model User {

  id       Int    @id @default(autoincrement())

  email    String @unique

  name     String

  password String

}


model Course {
  id          String    @id @default(uuid())
  title       String
  description String
  price       Int
}